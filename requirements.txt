# ─────────────────────────────────────────────────────────────────────────────
# NovaAI — BASE requirements (text chat + headless web UI).
#
# This set is intentionally small, pure-Python where possible, and installs on
# every platform NovaAI targets, including aarch64 / Raspberry Pi 5. It is all
# you need for the terminal chat loop (`python app.py`) and the browser-based
# web UI (`python app.py --web`).
#
# Voice (speech-in / speech-out / singing) and the native desktop GUI are
# optional add-ons — install them only if you want those features:
#   pip install -r requirements-voice.txt   # mic, STT, TTS/XTTS, embeddings
#   pip install -r requirements-gui.txt     # native pywebview desktop window
# ─────────────────────────────────────────────────────────────────────────────

python-dotenv>=1.0,<2
requests>=2.32,<3
duckduckgo-search>=6,<8
dateparser>=1.1,<2
numpy>=1.24,<3

# Lightweight, pure-Python WebSocket server used by the optional 3D avatar
# bridge (and harmless to keep in the base set on all platforms).
websockets>=11,<12
